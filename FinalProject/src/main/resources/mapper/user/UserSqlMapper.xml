<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ja.finalproject.user.mapper.UserSqlMapper">
   <insert id="insert">
      INSERT INTO fp_user VALUES(
         fp_user_seq.nextval,
         #{userid},
         #{password},
         #{nickname},
         #{email},
         #{gender},
         #{birth},
         #{phone},
         SYSDATE
      )
   </insert>
   
   <!-- 주이 사항: SELECT 경우... 꼭 resultType을 설정 -->
   
   <select id="selectByUserIdAndPassword" resultType="com.ja.finalproject.dto.UserDto">
	   SELECT * FROM fp_user
	   WHERE userid= #{userid} 
	   AND password =  #{password}
   </select>
   
   <select id="selectById" resultType="com.ja.finalproject.dto.UserDto">
   		SELECT * FROM FP_USER WHERE id = #{id}
   </select>
</mapper>